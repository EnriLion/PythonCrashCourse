packages:

# Matplotlib - make a simple plots such as line graphs and scatter plots

$ python -m pip install --user matplotlib
$ python3 -m pip install --user matplotlib

# Plotly - creates a visualization that work well on digital devices(generates visualizations that automatically resize to fit a variety of display devices; these includes a number of interactive features such as emphasizing particular aspects of the data set when users hover over different parts of the visualization.)

